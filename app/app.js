function main() {
  // Core Google Ads logic will be written here, 
  // referencing the `core` and `helper` files.
}

// Import your modules here. Note that this will still
// be parsed by the import module if commented  out.
@import './core/getDistinct.js';
@import './helper/getDisapprovedAds.js'